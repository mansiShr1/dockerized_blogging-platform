# dockerized_blogging-platform
This project is a containerized web app built using Docker. It uses multiple services (frontend, backend, database) running in separate containers. Users can add and view posts, ensuring easy deployment, scalability, and portability across systems.
